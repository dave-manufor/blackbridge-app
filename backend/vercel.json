{
  "version": 2,
  "buildCommand": "npm run prisma:generate && npm run prisma:migrate",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}
