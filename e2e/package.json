{
  "name": "e2e-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "pretest": "docker compose up -d --wait && cd ../backend && DATABASE_URL=postgresql://test_user:test_password@localhost:5433/test_db npx prisma db push --force-reset",
    "posttest": "docker compose down -v"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.0.0"
  }
}
